services:
  pythontelegrambot:
    image: pythontelegrambot
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      TZ: "${TZ}"
      BOT_TOKEN: "${BOT_TOKEN}"
      PYTHONPATH: "${PYTHONPATH}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
